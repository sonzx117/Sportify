import { Error, Loader } from '../components';
import { genres } from '../assets/constants';

const Discover = () => {
  console.log(genres);
  return (
    <div>discover</div>
  );
};
export default Discover;
